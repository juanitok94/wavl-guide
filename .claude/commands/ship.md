# Ship

Ask me for a one-line commit message describing what 
changed, then run:

git add .
git commit -m "[my message]"
git push

After pushing, confirm with the commit hash and 
"Deployed to Vercel in ~60s".
